# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# local build output (see `npm run build:android:local`)
*.apk

# Claude Code: kept out of the remote entirely by explicit request (this
# used to keep settings.json/skills/ui-ux-design/SKILL.md tracked as
# legitimate, secret-free project config -- both still exist locally, just
# no longer published to GitHub).
.claude/

# AI-assistant-generated planning/spec docs from past sessions -- kept
# locally for reference, not published to the remote (mentions another
# machine's local path).
docs/superpowers/

# Supabase CLI's own local cache dir (created by `npx supabase ...`)
supabase/.temp/
